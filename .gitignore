# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
venv/
env/
.env

# Release signing keys are never repository content. Public verification keys
# are explicitly allowlisted because they contain no signing capability.
*.pem
!backend/release_keys/*.public.pem

# Offline model artifacts are local research outputs only; Render and the browser path do not ship them.
# Offline model outputs and local dependency caches
backend/saved_models/
saved_models/
*.keras
.uv-cache/
frontend/.npm-cache/

*.csv
!data/ndx100/membership_changes.csv
!data/fixtures/synthetic_csco_like_golden_v1.csv
*.dat

# Generated data

# Logs
*.log

# Jupyter
.ipynb_checkpoints/

# OS and IDEs
.DS_Store
.vscode/
.idea/
diff.txt
.coverage
reports/*
!reports/empirical_volatility_benchmark_*.md
!reports/empirical_volatility_benchmark_*.json

# Local agent/tool state. Generated by the assistant tooling for workspace
# memory and session bookkeeping; it is never project source and must not be
# committed. The directory itself is preserved on disk.
.workbuddy-ai/

# Licensed or generated research snapshots
data/snapshots/
*.parquet
*.jsonl

# Local research outputs. These are diagnostic only and are never evidence, so
# they must never be committed. The quarantine marker below is re-included on
# purpose: it documents why a previously generated artifact is invalid.
artifacts/*
!artifacts/_quarantine_invalid_numeric_companion_v9/
artifacts/_quarantine_invalid_numeric_companion_v9/*
!artifacts/_quarantine_invalid_numeric_companion_v9/QUARANTINE.md
!artifacts/_quarantine_invalid_numeric_companion_v9/numeric_companion_freeze.json
research/results/ndx100-v9/

# Vitest/Node caches that may appear at repo root
node_modules/
